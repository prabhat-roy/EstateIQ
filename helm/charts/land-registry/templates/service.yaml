apiVersion: v1
kind: Service
metadata:
  name: {{ include "land-registry.fullname" . }}
  labels: {{- include "land-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "land-registry.selectorLabels" . | nindent 4 }}
