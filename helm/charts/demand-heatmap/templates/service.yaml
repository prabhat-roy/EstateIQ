apiVersion: v1
kind: Service
metadata:
  name: {{ include "demand-heatmap.fullname" . }}
  labels: {{- include "demand-heatmap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demand-heatmap.selectorLabels" . | nindent 4 }}
