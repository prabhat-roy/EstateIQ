apiVersion: v1
kind: Service
metadata:
  name: {{ include "deposit-service.fullname" . }}
  labels: {{- include "deposit-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "deposit-service.selectorLabels" . | nindent 4 }}
