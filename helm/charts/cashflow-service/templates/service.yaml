apiVersion: v1
kind: Service
metadata:
  name: {{ include "cashflow-service.fullname" . }}
  labels: {{- include "cashflow-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "cashflow-service.selectorLabels" . | nindent 4 }}
