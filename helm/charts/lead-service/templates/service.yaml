apiVersion: v1
kind: Service
metadata:
  name: {{ include "lead-service.fullname" . }}
  labels: {{- include "lead-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "lead-service.selectorLabels" . | nindent 4 }}
