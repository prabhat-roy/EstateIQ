apiVersion: v1
kind: Service
metadata:
  name: {{ include "conveyancing-service.fullname" . }}
  labels: {{- include "conveyancing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "conveyancing-service.selectorLabels" . | nindent 4 }}
