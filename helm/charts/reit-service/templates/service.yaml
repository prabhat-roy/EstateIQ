apiVersion: v1
kind: Service
metadata:
  name: {{ include "reit-service.fullname" . }}
  labels: {{- include "reit-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "reit-service.selectorLabels" . | nindent 4 }}
