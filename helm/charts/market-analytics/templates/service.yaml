apiVersion: v1
kind: Service
metadata:
  name: {{ include "market-analytics.fullname" . }}
  labels: {{- include "market-analytics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "market-analytics.selectorLabels" . | nindent 4 }}
