apiVersion: v1
kind: Service
metadata:
  name: {{ include "escrow-service.fullname" . }}
  labels: {{- include "escrow-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "escrow-service.selectorLabels" . | nindent 4 }}
