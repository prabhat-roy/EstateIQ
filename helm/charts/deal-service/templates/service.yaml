apiVersion: v1
kind: Service
metadata:
  name: {{ include "deal-service.fullname" . }}
  labels: {{- include "deal-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "deal-service.selectorLabels" . | nindent 4 }}
