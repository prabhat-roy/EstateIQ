apiVersion: v1
kind: Service
metadata:
  name: {{ include "price-prediction.fullname" . }}
  labels: {{- include "price-prediction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "price-prediction.selectorLabels" . | nindent 4 }}
