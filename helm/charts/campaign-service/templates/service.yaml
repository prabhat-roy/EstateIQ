apiVersion: v1
kind: Service
metadata:
  name: {{ include "campaign-service.fullname" . }}
  labels: {{- include "campaign-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "campaign-service.selectorLabels" . | nindent 4 }}
