apiVersion: v1
kind: Service
metadata:
  name: {{ include "avm-service.fullname" . }}
  labels: {{- include "avm-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "avm-service.selectorLabels" . | nindent 4 }}
