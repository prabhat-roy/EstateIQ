apiVersion: v1
kind: Service
metadata:
  name: {{ include "investor-bff.fullname" . }}
  labels: {{- include "investor-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "investor-bff.selectorLabels" . | nindent 4 }}
