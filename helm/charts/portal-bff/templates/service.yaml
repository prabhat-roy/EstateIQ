apiVersion: v1
kind: Service
metadata:
  name: {{ include "portal-bff.fullname" . }}
  labels: {{- include "portal-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "portal-bff.selectorLabels" . | nindent 4 }}
