apiVersion: v1
kind: Service
metadata:
  name: {{ include "contractor-service.fullname" . }}
  labels: {{- include "contractor-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "contractor-service.selectorLabels" . | nindent 4 }}
