apiVersion: v1
kind: Service
metadata:
  name: {{ include "investor-sso.fullname" . }}
  labels: {{- include "investor-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "investor-sso.selectorLabels" . | nindent 4 }}
