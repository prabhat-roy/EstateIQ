cloud:
  provider: aws
  storageClass: gp3
  ingressClass: alb

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::{{ .Values.aws.accountId }}:role/estateiq-media-service

podAnnotations:
  iam.amazonaws.com/role: estateiq-media-service

envFrom:
  - secretRef:
      name: media-service-aws-secrets
