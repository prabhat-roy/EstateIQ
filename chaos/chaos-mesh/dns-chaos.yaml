apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata: { name: estateiq-dns-error, namespace: chaos-mesh }
spec:
  action: error
  mode: all
  patterns: ["*.estateiq.svc.cluster.local"]
  selector: { namespaces: [estateiq] }
  duration: 2m
