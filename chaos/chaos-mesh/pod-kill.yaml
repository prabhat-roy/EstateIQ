apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata: { name: estateiq-pod-kill, namespace: chaos-mesh }
spec:
  action: pod-kill
  mode: one
  selector: { namespaces: [estateiq] }
  duration: 1m
  scheduler: { cron: "@every 1h" }
