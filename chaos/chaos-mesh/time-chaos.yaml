apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata: { name: estateiq-clock-skew, namespace: chaos-mesh }
spec:
  mode: one
  selector: { namespaces: [estateiq] }
  timeOffset: "+500ms"
  duration: 3m
