apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: invoice-service, estateiq.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: invoice-service }
