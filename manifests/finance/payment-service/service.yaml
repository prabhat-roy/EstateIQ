apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: payment-service, estateiq.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: payment-service }
