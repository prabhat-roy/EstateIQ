apiVersion: v1
kind: Service
metadata:
  name: accounting-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: accounting-service, estateiq.io/domain: finance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50202, targetPort: 50202 }]
  selector: { app.kubernetes.io/name: accounting-service }
