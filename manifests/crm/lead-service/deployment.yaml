apiVersion: apps/v1
kind: Deployment
metadata:
  name: lead-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: lead-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: crm
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lead-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lead-service, estateiq.io/domain: crm }
    spec:
      serviceAccountName: lead-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lead-service
          image: harbor.estateiq.internal/estateiq/lead-service:latest
          ports: [{ containerPort: 50181, name: http }]
          envFrom: [{ configMapRef: { name: lead-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50181 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
