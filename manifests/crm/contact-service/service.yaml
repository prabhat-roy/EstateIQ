apiVersion: v1
kind: Service
metadata:
  name: contact-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: contact-service, estateiq.io/domain: crm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: contact-service }
