apiVersion: apps/v1
kind: Deployment
metadata:
  name: contact-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: contact-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: crm
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: contact-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: contact-service, estateiq.io/domain: crm }
    spec:
      serviceAccountName: contact-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: contact-service
          image: harbor.estateiq.internal/estateiq/contact-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: contact-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
