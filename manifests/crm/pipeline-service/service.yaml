apiVersion: v1
kind: Service
metadata:
  name: pipeline-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: pipeline-service, estateiq.io/domain: crm }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: pipeline-service }
