apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipeline-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: pipeline-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: crm
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: pipeline-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: pipeline-service, estateiq.io/domain: crm }
    spec:
      serviceAccountName: pipeline-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: pipeline-service
          image: harbor.estateiq.internal/estateiq/pipeline-service:latest
          ports: [{ containerPort: 50182, name: http }]
          envFrom: [{ configMapRef: { name: pipeline-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50182 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
