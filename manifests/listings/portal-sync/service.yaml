apiVersion: v1
kind: Service
metadata:
  name: portal-sync
  namespace: estateiq
  labels: { app.kubernetes.io/name: portal-sync, estateiq.io/domain: listings }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50042, targetPort: 50042 }]
  selector: { app.kubernetes.io/name: portal-sync }
