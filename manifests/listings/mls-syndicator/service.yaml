apiVersion: v1
kind: Service
metadata:
  name: mls-syndicator
  namespace: estateiq
  labels: { app.kubernetes.io/name: mls-syndicator, estateiq.io/domain: listings }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: mls-syndicator }
