apiVersion: v1
kind: Service
metadata:
  name: virtual-tour
  namespace: estateiq
  labels: { app.kubernetes.io/name: virtual-tour, estateiq.io/domain: listings }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50043, targetPort: 50043 }]
  selector: { app.kubernetes.io/name: virtual-tour }
