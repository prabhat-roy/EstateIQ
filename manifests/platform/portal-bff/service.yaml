apiVersion: v1
kind: Service
metadata:
  name: portal-bff
  namespace: estateiq
  labels: { app.kubernetes.io/name: portal-bff, estateiq.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50003, targetPort: 50003 }]
  selector: { app.kubernetes.io/name: portal-bff }
