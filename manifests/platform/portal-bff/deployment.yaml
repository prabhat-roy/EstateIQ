apiVersion: apps/v1
kind: Deployment
metadata:
  name: portal-bff
  namespace: estateiq
  labels:
    app.kubernetes.io/name: portal-bff
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: portal-bff } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: portal-bff, estateiq.io/domain: platform }
    spec:
      serviceAccountName: portal-bff
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: portal-bff
          image: harbor.estateiq.internal/estateiq/portal-bff:latest
          ports: [{ containerPort: 50003, name: http }]
          envFrom: [{ configMapRef: { name: portal-bff } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50003 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50003 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
