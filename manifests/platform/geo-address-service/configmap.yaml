apiVersion: v1
kind: ConfigMap
metadata:
  name: geo-address-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: geo-address-service, estateiq.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51013"
