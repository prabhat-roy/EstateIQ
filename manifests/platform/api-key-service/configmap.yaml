apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: api-key-service, estateiq.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
