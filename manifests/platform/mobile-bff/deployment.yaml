apiVersion: apps/v1
kind: Deployment
metadata:
  name: mobile-bff
  namespace: estateiq
  labels:
    app.kubernetes.io/name: mobile-bff
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: mobile-bff } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: mobile-bff, estateiq.io/domain: platform }
    spec:
      serviceAccountName: mobile-bff
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: mobile-bff
          image: harbor.estateiq.internal/estateiq/mobile-bff:latest
          ports: [{ containerPort: 50004, name: http }]
          envFrom: [{ configMapRef: { name: mobile-bff } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50004 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50004 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
