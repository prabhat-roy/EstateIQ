apiVersion: v1
kind: Service
metadata:
  name: mobile-bff
  namespace: estateiq
  labels: { app.kubernetes.io/name: mobile-bff, estateiq.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50004, targetPort: 50004 }]
  selector: { app.kubernetes.io/name: mobile-bff }
