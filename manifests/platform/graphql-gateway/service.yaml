apiVersion: v1
kind: Service
metadata:
  name: graphql-gateway
  namespace: estateiq
  labels: { app.kubernetes.io/name: graphql-gateway, estateiq.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50005, targetPort: 50005 }]
  selector: { app.kubernetes.io/name: graphql-gateway }
