apiVersion: v1
kind: Service
metadata:
  name: agent-bff
  namespace: estateiq
  labels: { app.kubernetes.io/name: agent-bff, estateiq.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50001, targetPort: 50001 }]
  selector: { app.kubernetes.io/name: agent-bff }
