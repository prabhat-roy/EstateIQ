apiVersion: v1
kind: Service
metadata:
  name: investor-bff
  namespace: estateiq
  labels: { app.kubernetes.io/name: investor-bff, estateiq.io/domain: platform }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50002, targetPort: 50002 }]
  selector: { app.kubernetes.io/name: investor-bff }
