apiVersion: apps/v1
kind: Deployment
metadata:
  name: investor-bff
  namespace: estateiq
  labels:
    app.kubernetes.io/name: investor-bff
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: platform
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: investor-bff } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: investor-bff, estateiq.io/domain: platform }
    spec:
      serviceAccountName: investor-bff
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: investor-bff
          image: harbor.estateiq.internal/estateiq/investor-bff:latest
          ports: [{ containerPort: 50002, name: http }]
          envFrom: [{ configMapRef: { name: investor-bff } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50002 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50002 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
