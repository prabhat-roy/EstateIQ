apiVersion: v1
kind: Service
metadata:
  name: archive-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: archive-service, estateiq.io/domain: document }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50282, targetPort: 50282 }]
  selector: { app.kubernetes.io/name: archive-service }
