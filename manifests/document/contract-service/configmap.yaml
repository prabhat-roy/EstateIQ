apiVersion: v1
kind: ConfigMap
metadata:
  name: contract-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: contract-service, estateiq.io/domain: document }
data:
  LOG_LEVEL: info
  PORT: "50281"
