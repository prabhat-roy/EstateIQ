apiVersion: v1
kind: Service
metadata:
  name: work-order-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: work-order-service, estateiq.io/domain: facilities }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50121, targetPort: 50121 }]
  selector: { app.kubernetes.io/name: work-order-service }
