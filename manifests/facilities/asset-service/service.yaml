apiVersion: v1
kind: Service
metadata:
  name: asset-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: asset-service, estateiq.io/domain: facilities }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50120, targetPort: 50120 }]
  selector: { app.kubernetes.io/name: asset-service }
