apiVersion: v1
kind: Service
metadata:
  name: contractor-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: contractor-service, estateiq.io/domain: facilities }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: contractor-service }
