apiVersion: v1
kind: Service
metadata:
  name: tenant-sso
  namespace: estateiq
  labels: { app.kubernetes.io/name: tenant-sso, estateiq.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50023, targetPort: 50023 }]
  selector: { app.kubernetes.io/name: tenant-sso }
