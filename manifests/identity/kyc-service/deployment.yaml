apiVersion: apps/v1
kind: Deployment
metadata:
  name: kyc-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: kyc-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: kyc-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: kyc-service, estateiq.io/domain: identity }
    spec:
      serviceAccountName: kyc-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: kyc-service
          image: harbor.estateiq.internal/estateiq/kyc-service:latest
          ports: [{ containerPort: 50025, name: http }]
          envFrom: [{ configMapRef: { name: kyc-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50025 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50025 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
