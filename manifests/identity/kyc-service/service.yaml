apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: kyc-service, estateiq.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50025, targetPort: 50025 }]
  selector: { app.kubernetes.io/name: kyc-service }
