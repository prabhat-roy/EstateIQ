apiVersion: v1
kind: Service
metadata:
  name: investor-sso
  namespace: estateiq
  labels: { app.kubernetes.io/name: investor-sso, estateiq.io/domain: identity }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50022, targetPort: 50022 }]
  selector: { app.kubernetes.io/name: investor-sso }
