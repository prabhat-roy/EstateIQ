apiVersion: apps/v1
kind: Deployment
metadata:
  name: deal-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: deal-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: transactions
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: deal-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: deal-service, estateiq.io/domain: transactions }
    spec:
      serviceAccountName: deal-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: deal-service
          image: harbor.estateiq.internal/estateiq/deal-service:latest
          ports: [{ containerPort: 50060, name: http }]
          envFrom: [{ configMapRef: { name: deal-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50060 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
