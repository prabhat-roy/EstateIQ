apiVersion: v1
kind: Service
metadata:
  name: deal-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: deal-service, estateiq.io/domain: transactions }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50060, targetPort: 50060 }]
  selector: { app.kubernetes.io/name: deal-service }
