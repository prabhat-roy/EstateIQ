apiVersion: v1
kind: Service
metadata:
  name: conveyancing-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: conveyancing-service, estateiq.io/domain: transactions }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: conveyancing-service }
