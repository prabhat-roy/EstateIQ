apiVersion: apps/v1
kind: Deployment
metadata:
  name: conveyancing-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: conveyancing-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: transactions
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: conveyancing-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: conveyancing-service, estateiq.io/domain: transactions }
    spec:
      serviceAccountName: conveyancing-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: conveyancing-service
          image: harbor.estateiq.internal/estateiq/conveyancing-service:latest
          ports: [{ containerPort: 50062, name: http }]
          envFrom: [{ configMapRef: { name: conveyancing-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50062 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
