apiVersion: v1
kind: ConfigMap
metadata:
  name: closing-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: closing-service, estateiq.io/domain: transactions }
data:
  LOG_LEVEL: info
  PORT: "50064"
