apiVersion: v1
kind: Service
metadata:
  name: closing-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: closing-service, estateiq.io/domain: transactions }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50064, targetPort: 50064 }]
  selector: { app.kubernetes.io/name: closing-service }
