apiVersion: apps/v1
kind: Deployment
metadata:
  name: offer-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: offer-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: transactions
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: offer-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: offer-service, estateiq.io/domain: transactions }
    spec:
      serviceAccountName: offer-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: offer-service
          image: harbor.estateiq.internal/estateiq/offer-service:latest
          ports: [{ containerPort: 50061, name: http }]
          envFrom: [{ configMapRef: { name: offer-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50061 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
