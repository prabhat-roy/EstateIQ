apiVersion: v1
kind: Service
metadata:
  name: offer-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: offer-service, estateiq.io/domain: transactions }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50061, targetPort: 50061 }]
  selector: { app.kubernetes.io/name: offer-service }
