apiVersion: v1
kind: Service
metadata:
  name: escrow-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: escrow-service, estateiq.io/domain: transactions }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50063, targetPort: 50063 }]
  selector: { app.kubernetes.io/name: escrow-service }
