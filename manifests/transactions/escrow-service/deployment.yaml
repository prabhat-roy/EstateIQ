apiVersion: apps/v1
kind: Deployment
metadata:
  name: escrow-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: escrow-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: transactions
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: escrow-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: escrow-service, estateiq.io/domain: transactions }
    spec:
      serviceAccountName: escrow-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: escrow-service
          image: harbor.estateiq.internal/estateiq/escrow-service:latest
          ports: [{ containerPort: 50063, name: http }]
          envFrom: [{ configMapRef: { name: escrow-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50063 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
