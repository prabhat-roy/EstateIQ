apiVersion: v1
kind: ConfigMap
metadata:
  name: escrow-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: escrow-service, estateiq.io/domain: transactions }
data:
  LOG_LEVEL: info
  PORT: "50063"
