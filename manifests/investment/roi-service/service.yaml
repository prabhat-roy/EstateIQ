apiVersion: v1
kind: Service
metadata:
  name: roi-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: roi-service, estateiq.io/domain: investment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: roi-service }
