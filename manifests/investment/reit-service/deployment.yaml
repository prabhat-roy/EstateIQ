apiVersion: apps/v1
kind: Deployment
metadata:
  name: reit-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: reit-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: investment
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: reit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: reit-service, estateiq.io/domain: investment }
    spec:
      serviceAccountName: reit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: reit-service
          image: harbor.estateiq.internal/estateiq/reit-service:latest
          ports: [{ containerPort: 50161, name: http }]
          envFrom: [{ configMapRef: { name: reit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50161 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
