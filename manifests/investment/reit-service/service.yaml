apiVersion: v1
kind: Service
metadata:
  name: reit-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: reit-service, estateiq.io/domain: investment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50161, targetPort: 50161 }]
  selector: { app.kubernetes.io/name: reit-service }
