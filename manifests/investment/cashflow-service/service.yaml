apiVersion: v1
kind: Service
metadata:
  name: cashflow-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: cashflow-service, estateiq.io/domain: investment }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50163, targetPort: 50163 }]
  selector: { app.kubernetes.io/name: cashflow-service }
