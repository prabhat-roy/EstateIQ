apiVersion: v1
kind: Service
metadata:
  name: title-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: title-service, estateiq.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50221, targetPort: 50221 }]
  selector: { app.kubernetes.io/name: title-service }
