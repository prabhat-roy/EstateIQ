apiVersion: v1
kind: Service
metadata:
  name: sanction-screening
  namespace: estateiq
  labels: { app.kubernetes.io/name: sanction-screening, estateiq.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: sanction-screening }
