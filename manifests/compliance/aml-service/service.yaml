apiVersion: v1
kind: Service
metadata:
  name: aml-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: aml-service, estateiq.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: aml-service }
