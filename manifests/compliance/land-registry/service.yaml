apiVersion: v1
kind: Service
metadata:
  name: land-registry
  namespace: estateiq
  labels: { app.kubernetes.io/name: land-registry, estateiq.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50223, targetPort: 50223 }]
  selector: { app.kubernetes.io/name: land-registry }
