apiVersion: v1
kind: Service
metadata:
  name: tenant-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: tenant-service, estateiq.io/domain: leasing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50081, targetPort: 50081 }]
  selector: { app.kubernetes.io/name: tenant-service }
