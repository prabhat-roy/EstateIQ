apiVersion: apps/v1
kind: Deployment
metadata:
  name: renewal-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: renewal-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: leasing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: renewal-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: renewal-service, estateiq.io/domain: leasing }
    spec:
      serviceAccountName: renewal-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: renewal-service
          image: harbor.estateiq.internal/estateiq/renewal-service:latest
          ports: [{ containerPort: 50083, name: http }]
          envFrom: [{ configMapRef: { name: renewal-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50083 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50083 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
