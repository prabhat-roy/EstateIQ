apiVersion: v1
kind: Service
metadata:
  name: renewal-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: renewal-service, estateiq.io/domain: leasing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50083, targetPort: 50083 }]
  selector: { app.kubernetes.io/name: renewal-service }
