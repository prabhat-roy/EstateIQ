apiVersion: apps/v1
kind: Deployment
metadata:
  name: lease-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: lease-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: leasing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lease-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lease-service, estateiq.io/domain: leasing }
    spec:
      serviceAccountName: lease-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lease-service
          image: harbor.estateiq.internal/estateiq/lease-service:latest
          ports: [{ containerPort: 50080, name: http }]
          envFrom: [{ configMapRef: { name: lease-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50080 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50080 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
