apiVersion: v1
kind: Service
metadata:
  name: lease-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: lease-service, estateiq.io/domain: leasing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50080, targetPort: 50080 }]
  selector: { app.kubernetes.io/name: lease-service }
