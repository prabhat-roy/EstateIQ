apiVersion: apps/v1
kind: Deployment
metadata:
  name: rent-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: rent-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: leasing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: rent-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: rent-service, estateiq.io/domain: leasing }
    spec:
      serviceAccountName: rent-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: rent-service
          image: harbor.estateiq.internal/estateiq/rent-service:latest
          ports: [{ containerPort: 50082, name: http }]
          envFrom: [{ configMapRef: { name: rent-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50082 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
