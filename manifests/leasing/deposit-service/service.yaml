apiVersion: v1
kind: Service
metadata:
  name: deposit-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: deposit-service, estateiq.io/domain: leasing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50084, targetPort: 50084 }]
  selector: { app.kubernetes.io/name: deposit-service }
