apiVersion: apps/v1
kind: Deployment
metadata:
  name: deposit-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: deposit-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: leasing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: deposit-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: deposit-service, estateiq.io/domain: leasing }
    spec:
      serviceAccountName: deposit-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: deposit-service
          image: harbor.estateiq.internal/estateiq/deposit-service:latest
          ports: [{ containerPort: 50084, name: http }]
          envFrom: [{ configMapRef: { name: deposit-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50084 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50084 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
