apiVersion: v1
kind: Service
metadata:
  name: milestone-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: milestone-service, estateiq.io/domain: construction }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50141, targetPort: 50141 }]
  selector: { app.kubernetes.io/name: milestone-service }
