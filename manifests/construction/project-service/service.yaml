apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: project-service, estateiq.io/domain: construction }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50140, targetPort: 50140 }]
  selector: { app.kubernetes.io/name: project-service }
