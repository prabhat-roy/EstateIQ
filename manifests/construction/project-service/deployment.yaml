apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: project-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: construction
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: project-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: project-service, estateiq.io/domain: construction }
    spec:
      serviceAccountName: project-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: project-service
          image: harbor.estateiq.internal/estateiq/project-service:latest
          ports: [{ containerPort: 50140, name: http }]
          envFrom: [{ configMapRef: { name: project-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50140 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
