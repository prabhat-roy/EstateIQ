apiVersion: v1
kind: Service
metadata:
  name: budget-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: budget-service, estateiq.io/domain: construction }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50142, targetPort: 50142 }]
  selector: { app.kubernetes.io/name: budget-service }
