apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: budget-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: construction
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: budget-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: budget-service, estateiq.io/domain: construction }
    spec:
      serviceAccountName: budget-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: budget-service
          image: harbor.estateiq.internal/estateiq/budget-service:latest
          ports: [{ containerPort: 50142, name: http }]
          envFrom: [{ configMapRef: { name: budget-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50142 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
