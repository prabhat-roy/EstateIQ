apiVersion: v1
kind: Service
metadata:
  name: portal-integration
  namespace: estateiq
  labels: { app.kubernetes.io/name: portal-integration, estateiq.io/domain: marketing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: portal-integration }
