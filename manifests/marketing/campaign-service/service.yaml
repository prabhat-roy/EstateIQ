apiVersion: v1
kind: Service
metadata:
  name: campaign-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: campaign-service, estateiq.io/domain: marketing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: campaign-service }
