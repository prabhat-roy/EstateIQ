apiVersion: v1
kind: Service
metadata:
  name: access-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: access-service, estateiq.io/domain: smart-building }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: access-service }
