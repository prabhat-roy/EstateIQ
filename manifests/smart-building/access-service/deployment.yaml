apiVersion: apps/v1
kind: Deployment
metadata:
  name: access-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: access-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: smart-building
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: access-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: access-service, estateiq.io/domain: smart-building }
    spec:
      serviceAccountName: access-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: access-service
          image: harbor.estateiq.internal/estateiq/access-service:latest
          ports: [{ containerPort: 50242, name: http }]
          envFrom: [{ configMapRef: { name: access-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50242 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
