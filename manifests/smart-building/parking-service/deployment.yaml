apiVersion: apps/v1
kind: Deployment
metadata:
  name: parking-service
  namespace: estateiq
  labels:
    app.kubernetes.io/name: parking-service
    app.kubernetes.io/part-of: estateiq
    estateiq.io/domain: smart-building
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: parking-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: parking-service, estateiq.io/domain: smart-building }
    spec:
      serviceAccountName: parking-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: parking-service
          image: harbor.estateiq.internal/estateiq/parking-service:latest
          ports: [{ containerPort: 50243, name: http }]
          envFrom: [{ configMapRef: { name: parking-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50243 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
