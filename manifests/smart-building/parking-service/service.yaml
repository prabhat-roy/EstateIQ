apiVersion: v1
kind: Service
metadata:
  name: parking-service
  namespace: estateiq
  labels: { app.kubernetes.io/name: parking-service, estateiq.io/domain: smart-building }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50243, targetPort: 50243 }]
  selector: { app.kubernetes.io/name: parking-service }
