apiVersion: v1
kind: Service
metadata:
  name: iot-gateway
  namespace: estateiq
  labels: { app.kubernetes.io/name: iot-gateway, estateiq.io/domain: smart-building }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: iot-gateway }
