apiVersion: v1
kind: Service
metadata:
  name: market-analytics
  namespace: estateiq
  labels: { app.kubernetes.io/name: market-analytics, estateiq.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: market-analytics }
