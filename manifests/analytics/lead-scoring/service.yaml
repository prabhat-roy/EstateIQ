apiVersion: v1
kind: Service
metadata:
  name: lead-scoring
  namespace: estateiq
  labels: { app.kubernetes.io/name: lead-scoring, estateiq.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: lead-scoring }
