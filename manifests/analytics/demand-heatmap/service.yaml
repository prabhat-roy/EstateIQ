apiVersion: v1
kind: Service
metadata:
  name: demand-heatmap
  namespace: estateiq
  labels: { app.kubernetes.io/name: demand-heatmap, estateiq.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: demand-heatmap }
