apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: energy-service
  namespace: estateiq
spec:
  replicas: 3
  strategy:
    canary:
      steps:
        - setWeight: 10
        - pause: { duration: 5m }
        - analysis:
            templates: [{ templateName: estateiq-canary-analysis }]
            args:
              - { name: service-name, value: energy-service }
        - setWeight: 25
        - pause: { duration: 5m }
        - setWeight: 50
        - pause: { duration: 5m }
        - setWeight: 100
  selector:
    matchLabels: { app.kubernetes.io/name: energy-service }
  template:
    metadata:
      labels: { app.kubernetes.io/name: energy-service }
    spec:
      containers:
        - name: energy-service
          image: harbor.estateiq.internal/estateiq/energy-service:latest
          ports: [{ containerPort: 8080 }]
          readinessProbe: { httpGet: { path: /healthz, port: 8080 }, initialDelaySeconds: 5, periodSeconds: 10 }
          livenessProbe:  { httpGet: { path: /healthz, port: 8080 }, initialDelaySeconds: 15, periodSeconds: 30 }
