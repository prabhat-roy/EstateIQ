apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata: { name: estateiq, namespace: flux-system }
spec:
  interval: 1m
  url: https://github.com/prabhat-roy/EstateIQ.git
  ref: { branch: main }