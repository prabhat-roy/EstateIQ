# Apache Polaris — Iceberg REST catalog with RBAC + multi-engine support
image: { repository: apache/polaris, tag: latest }
polaris:
  realm: estateiq
  storage:
    s3: { endpoint: http://minio.estateiq.svc:9000, region: us-east-1, bucket: estateiq-iceberg-warehouse }
ingress: { enabled: true, hosts: [{ host: polaris.estateiq.internal }] }
