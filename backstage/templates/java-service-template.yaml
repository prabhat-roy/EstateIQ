apiVersion: scaffolder.backstage.io/v1beta3
kind: Template
metadata: { name: estateiq_java_service, title: New EstateIQ Java/Spring service, tags: [java, estateiq] }
spec:
  owner: platform-team
  type: service
  parameters:
    - title: Service info
      required: [name]
      properties: { name: { title: Name, type: string } }
  steps:
    - { id: fetch, name: Fetch, action: fetch:template, input: { url: ./skeletons/java } }
