estateiq:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres.estateiq.svc
      user: dbt
      password: "{{ env_var('DBT_PASSWORD') }}"
      port: 5432
      dbname: estateiq_warehouse
      schema: public
      threads: 8
    prod:
      type: trino
      host: trino.estateiq.svc
      port: 8080
      catalog: iceberg
      schema: estateiq_mart
      threads: 16
