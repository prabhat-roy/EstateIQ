# Sourcegraph (OSS) — universal code search across EstateIQ
image: { repository: sourcegraph/server, tag: 5.5.0 }
sourcegraph:
  externalURL: https://sourcegraph.estateiq.internal
  repositoriesToAdd: [github.com/prabhat-roy/EstateIQ]
ingress: { enabled: true, hosts: [{ host: sourcegraph.estateiq.internal }] }
