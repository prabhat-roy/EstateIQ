name: estateiq
description: EstateIQ — Pulumi alternative IaC (mirrors infra/terraform/{aws,gcp,azure})
runtime: { name: nodejs, options: { typescript: true } }
backend:
  url: s3://estateiq-pulumi-state
config:
  estateiq:env: prod
  estateiq:cloud: aws
