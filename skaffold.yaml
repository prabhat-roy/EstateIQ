# Skaffold pipeline for EstateIQ
apiVersion: skaffold/v4beta11
kind: Config
metadata: { name: estateiq }
build:
  artifacts:
    - { image: estateiq/api-gateway, context: src/api-gateway }
    - { image: estateiq/identity-service, context: src/identity-service }
  local: { push: false }
deploy:
  helm:
    releases:
      - { name: api-gateway, chartPath: helm/charts/api-gateway, namespace: estateiq }
profiles:
  - name: prod
    deploy: { kubectl: { manifests: ["manifests/prod/*.yaml"] } }
